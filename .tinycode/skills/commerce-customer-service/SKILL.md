# Commerce Agent 工作流

当用户询问商品、规格、材质、使用方法、售后或退款时：

1. 先用 `get_product_detail` 或 `search_products` 获取结构化商品事实。
2. 再用 `search_product_knowledge` 查询商品说明、售后政策和规则文档。
3. 只引用工具返回的事实；没有命中资料时明确说明无法确认。
4. 回复分成：已确认信息、客服回复、需要人工确认的事项。
5. 退款、改地址、发送外部消息等状态变更动作必须等待人工审批，当前版本不直接执行。
