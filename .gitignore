node_modules/
dist/
.tmp/
*.log
.DS_Store

# generated local RAG index (build via npm run commerce:ingest)
data/*.sqlite
data/*.sqlite-*

# secrets & local credentials — never commit
.env
.env.*
*.pem
*.key
.tinycode/*.local.json

# zread working state (in-repo copy lives in docs/wiki)
.zread/
